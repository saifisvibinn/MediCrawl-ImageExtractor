# This is a template for .streamlit/secrets.toml
# Copy this to .streamlit/secrets.toml and add your actual values
# DO NOT commit secrets.toml to version control!

# Example: Add password protection (requires custom implementation)
[passwords]
admin = "your-admin-password"

# Example: Add API keys if you extend the app
# [api_keys]
# openai_api_key = "sk-..."
# google_cloud_api_key = "..."

# Note: This file is for local development only
# On Streamlit Cloud, add secrets via the web interface:
# App settings → Secrets
